type Query {
    name(name: String): String!
        user: [users!] !
}

type Mutation {
    createUser(data: nameInput!): users!
    updateUser(oldName:String! , newName:String!):[users!] 
}
input nameInput {
    name: String!
}
type users {
    name(name: String): String!
}